sonar.projectKey=ThesisCentralServer
sonar.projectName=ThesisCentralServer
sonar.projectVersion=1.0
sonar.exclusions=**/*.js, **/*.xml

# Comma-separated paths to directories with sources (required)
sonar.sources=src
